export interface GeneralItemType {
  id: number;
  name: string;
}

export interface GeneralDbItemType {
  Id: number;
  Name: string;
}

export interface UpdateDialogProps {
  registration: any;
}

export interface AlertProps {
  action: any;
  id: number;
}